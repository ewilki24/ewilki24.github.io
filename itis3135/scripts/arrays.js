let name = [];
let salary = [];

function displayResults() {
    let s = document.getElementById(results);
    let result = s/7;
    alert(result);
}

function displaySalary() {
    let ns = document.getElementById(results_table);
    return ns;
}

function addSalary() {
    for (i = 0; i < name.length(); i++) {
        add.getElementById(new_name);
    }

    for(i = 0; i < salary.length(); i++) {
        add.getElementById(new_salary);
    }
}